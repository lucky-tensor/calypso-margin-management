{
  "name": "server",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "bun run --hot src/index.ts",
    "build": "bun build src/index.ts --target=bun --outdir=dist"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "postgres": "^3.4.8",
    "typescript": "^5.0.0"
  },
  "dependencies": {
    "core": "workspace:*",
    "db": "workspace:*",
    "rss-parser": "^3.13.0"
  }
}
